[project]
name = "qfeed-vllm-server"
version = "0.1.0"
description = "Q-feed 기술 면접 서비스를 위한 vLLM 기반 LLM 서빙 서버"
requires-python = ">=3.12"
dependencies = [
    "vllm==0.15.0",
    "transformers==4.56.0",
    "torch==2.9.1",
]

[tool.uv]
torch-backend = "auto"
